import React from "react";
import ReactDOM from "react-dom/client";
import GreenSyncApp from "./App";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <GreenSyncApp />
  </React.StrictMode>
);
